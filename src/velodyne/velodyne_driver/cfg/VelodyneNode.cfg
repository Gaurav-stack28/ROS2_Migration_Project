#!/usr/bin/env python3
#
# -----------------------------------------------------------------------------
#  ROS2 Migration Note
# -----------------------------------------------------------------------------
#  Package: velodyne_driver
#  Original file: VelodyneNode.cfg (ROS1 dynamic_reconfigure)
#
#  ROS2 does not support dynamic_reconfigure.
#  The parameters defined here are now standard ROS2 node parameters declared
#  in driver.cc using rclcpp::Node::declare_parameter().
#
#  This file is intentionally kept to preserve the original package structure
#  and to document the migrated parameters.
# -----------------------------------------------------------------------------

PACKAGE = "velodyne_driver"
NODE_NAME = "velodyne_node"

PARAMETERS = {
    "time_offset": {
        "type": "double",
        "default": 0.0,
        "min": -1.0,
        "max": 1.0,
        "description": "Offset (seconds) added to packet timestamps."
    },

    "enabled": {
        "type": "bool",
        "default": True,
        "description": "Enable or disable Velodyne packet processing."
    }
}

if __name__ == "__main__":
    print("ROS2 parameter documentation for velodyne_driver")
    for name, info in PARAMETERS.items():
        print(f"{name}: {info}")